apiVersion: v1
kind: Service
metadata:
  name: k8s-api-lb
  namespace: kube-system
spec:
  type: LoadBalancer
  ports:
    - port: 6443
      targetPort: 6443
      protocol: TCP
      name: https-api
  externalTrafficPolicy: Local
  selector:
    component: kube-apiserver